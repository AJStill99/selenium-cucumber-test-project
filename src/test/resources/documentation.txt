28/04/2026:

Added the dependencies needed for this (Selenium-java, cucumber-java, cucumber-junit, junit)
Also added in the maven-surefire-plugin + maven-compiler-plugin into plugins

From this, created the test runner, the features. and stepdefs
The above is I needed to be able to run tests using cucumber

09/05/2026

Developed the login feature. This included:
- Getting locators for the login page
- Storing these on the LoginPage.java, and creating methods using those
- Refactoring the stepdefs file to include valid steps for the login page
- Re-writing the feature file to include these new steps

Changed the base_url so the configReader could pass it into DriverManager
Testing Pull Request functionality. Part 2

10/05/2026

- Added in navigation during the actual stepdefs, not in the @Before hook - helps with flexibility.
- Set navigation class to have a valid driver in the @Before hook of the stepdefs file.
- Added a 'Routing' class, which includes a class of String values for the URL extensions
- Call navigation in first stepdef so the test has the shared state of the driver